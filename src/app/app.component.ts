import { Component } from '@angular/core';

@Component({
  selector: 'root',
  template: "<router-outlet></router-outlet>" //kullanıcı url den ne girerse o component çağırır
  
})
export class AppComponent {
  
}
